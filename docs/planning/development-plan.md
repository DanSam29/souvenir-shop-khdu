## Етап 0: Підготовка та вирівнювання ✅
- Провести рев’ю відповідності поточного коду ER-моделі; узгови полів/enum/зв’язки.  
- Зібрати міграції БД до актуального стану; підготувати скрипти seed дити наз(ролі, базові категорії, тестові товари).  
- Завершити опис Swagger для всіх публічних/адміністративних endpoint за таблицею інтерфейсів.  
- Зафіксувати карту помилок/типи відповіді (ValidationError, NotFound, Conflict, IntegrationError) для стабільної UX та логування.  
- Увімкнути фічефлаги для інтеграцій (Stripe/Nova/University) в конфігурації, щоб безпечно розгортати поступово.  
## Етап 1: Оплати (Stripe + COD) ✅
- Backend: завершити створення checkout-session; зберігати stripePaymentIntentId/SessionId в Payments; застосувати idempotency ключі.  
- Webhook: перевірка підпису (Webhook Secret), обробка payment_intent.succeeded/payment_intent.payment_failed, оновлення Payments.status і Orders.status, вставка OrderHistory.  
- Refund: реалізувати скасування оплачених замовлень з викликом Stripe refund; оновлювати OrderHistory та статуси.  
- COD: при Delivered адміністратором оновити Payment.status=Completed; зафіксувати в OrderHistory.  
- Frontend: у checkout показувати методи оплати, редірект на paymentUrl для картки; обробка success/cancel; відображення статусу.  
- Валідації: сума платежу має дорівнювати Order.totalAmount; заборонити повторний webhook на той самий Payment (ідемпотентність).  
- Тести: юніт PaymentService (success, fail, refund), інтеграційні зі Stripe sandbox, е2е checkout (CARD/COD).  
- Критерій: оплата карткою та при отриманні проходить е2е; fail/abort коректно скасовує замовлення і повертає stock.  
## Етап 2: Доставка Nova Poshta ✅
- Backend: endpoints для cities/warehouses/calculate; кешування списків; rate limit/ретраї; graceful fallback (ручне введення, якщо API недоступний).  
- Calculate: узгодити формулу вартості з вагами Products.weight; округлення до копійок; зберігати DeliveryCost.  
- Shipping збережені адреси: підтримати userId варіанти з isDefault; перевірка форматів phone; поля cityRef/warehouseRef.  
- Адмін: поле для ТТН у зміні статусу “Відправлено”; запис у OrderHistory.comment і Shipping.trackingNumber.  
- Frontend: автокомпліт міст/відділень; відображення вартості та орієнтовних днів; трекінг у кабінеті.  
- Тести: mock Nova Poshta; обробка недоступності; перерахунок вартості для різних ваг.  
- Критерій: вибір відділення і калькуляція працюють стабільно; ТТН зберігається, статуси видно у кабінеті.  
## Етап 3: Життєвий цикл замовлення і транзакції ✅
- Транзакційний createOrder: вставки Orders, OrderItems (originalPrice/appliedPromotionId/discountAmount/finalPrice), Shipping, Payments; генерація orderNumber; коміт лише після успішного створення пов’язаних записів.  
- Резерв товарів: створити OutgoingDocuments reason=ORDER з копіюванням знижок з OrderItems; зменшити Products.Stock.  
- Скасування: видалити OutgoingDocuments reason=ORDER, відновити Stock, оновити Order.status=Cancelled; при оплачених - refund; надіслати email.  
- Admin статуси: таблиця замовлень, зміна статусу Processing→Shipped→Delivered; коментар (включно ТТН); історія змін.  
- Cart: очищення кошика після успішного оформлення.  
- Тести: транзакційні тести на атомарність, конкурентні сценарії (одночасні покупки одного SKU), скасування з відновленням stock.  
- Критерій: замовлення проходить усі стани за діаграмою станів Order; дані в складських документах і OrderHistory повні.  
## Етап 4: Складський облік ✅
- Форми прибуткових/видаткових накладних: CRUD; валідації (reason=RETURN вимагає companyId; для інших reason companyId=NULL); логування createdBy.  
- Компанії-постачальники: CRUD, деактивація (м’яка), унікальність name/email; фільтри/пошук; заборона використання деактивованих у нових накладних.  
- Поточні залишки: агрегати totalIncoming/totalOutgoing/currentStock; індикатори low-stock; експорт CSV/PDF.  
- Тести: валідації правил reason, перерахунок stock після документів, експорт.  
- Критерій: коректні залишки та історія складу; автоматичні видаткові з замовлень містять поля знижок.  
## Етап 5: Знижки і промокоди ✅
- Двигун застосування: зібрати перелік applicable знижок (персональні/студентські/товарні/категорійні/кошик); відсортувати за пріоритетами; вибрати найвигіднішу на лінійку; потім застосувати промокод поверх.  
- Промокоди: перевірки існування/активності/дат/лімітів/умов; currentUsage++; prevent abuse (rate limit, one-time per order).  
- Персистенція: у OrderItems та OutgoingDocuments зберігати appliedPromotionId/discountAmount/finalPrice для аудиту та аналітики.  
- Особисті знижки: призначення, usedCount; сторінки “Мої знижки” й історія використання.  
- Тести: комбінації знижок, коректні тотали, промокод невалідний сценарії, ліміти.  
- Критерій: е2е з кількома знижками і промокодом; адмін бачить статистику й ROI.  
## Етап 6: University API (студентські знижки) ✅
- Автоверифікація при реєстрації з @ksu.edu.ua/@student.ksu.edu.ua; отримання studentStatus/GPA; призначення знижок за правилами; термін дії (4 міс).  
- Queue ретраїв при недоступності; перегляд/ручні дії адміністратором: approve/reject/retry/extend; сторінки активних студентів та історії верифікацій.  
- Логування запитів; сторінка статусів інтеграцій з індикаторами.  
- Тести: stub API; позитивні/негативні сценарії; expiry.  
- Критерій: студентський статус призначається автоматично; ручні дії працюють; черга обробляється.  
## Етап 7: Нотифікації Email ✅
- Шаблони: підтвердження замовлення (деталізація знижок і доставки), успішна/невдала оплата, відправлено/доставлено, студентські знижки.  
- Ідемпотентність: захист від дублювання відправок; ретраї; логування результатів.  
- Тести: генерація шаблонів, надсилання, локалізація.  
- Критерій: користувач отримує листи в ключові моменти; зміст коректний.  
## Етап 8: Каталог і медіа ✅
- Product/Category CRUD: повні форми, фільтри, сортування; drag-and-drop дерева категорій.  
- Зображення: завантаження до MinIO, встановлення головного, порядок; валідації форматів/розмірів; очищення при видаленні.  
- Кеш каталогу: інвалізація при зміні товарів/категорій; TTL для публічних списків; пошук full-text і комбіновані фільтри.  
- Тести: завантаження/видалення зображень, кеш-інвалізація, пошук/фільтри.  
- Критерій: менеджер керує каталогом; користувач швидко знаходить і переглядає товари.  
## Етап 9: Особистий кабінет користувача ✅
- Профіль: редагування даних, зміна паролю, перегляд студентського статусу.  
- Замовлення: історія, деталізація, відстеження; прогрес-бар статусів.  
- Адреси: додавання/видалення, isDefault, реюз у checkout.  
- Знижки: список доступних, історія використання.  
- Тести: UI е2е сценарії профілю/замовлень/адрес/знижок.  
- Критерій: користувач самостійно виконує весь цикл покупки й керує профілем.  
## Етап 10: Адмін-панель ✅
- Замовлення: таблиця, фільтри, деталі, зміна статусів, коментарі/ТТН, скасування з опціональним refund.  
- Користувачі: список, фільтри, блокування/розблокування, скидання паролю; перегляд замовлень користувача.  
- Склад: накладні, поточні залишки, компанії, експорт.  
- Акції: CRUD, статистика, призначення персональних знижок.  
- Інтеграції: статус-дашборд Stripe/Nova/University з тестами і логами.  
- Тести: доступи RBAC, операції, експорт.  
- Критерій: адміністратор і менеджер виконують свої задачі без залучення розробника.  
## Етап 11: Аналітика і звітність ✅
- Продажі/дохід/середній чек; популярність товарів і категорій; ефективність акцій; фінансові звіти (дохід, витрати, прибуток, margin, ROI).  
- Кешування агрегатів; фільтри періодів; графіки; експорт PDF/CSV/Excel.  
- Тести: перевірка запитів/агрегатів; правильність експортів.  
- Критерій: дашборди відображають коректні метрики; експорт працює.  
## Етап 12: Безпека та NFR ✅
- HTTPS, HSTS; JWT + RBAC; CORS; CSRF; rate limiting; валідація всіх DTO (FluentValidation).  
- Секрети: лише в env; роль SuperAdmin керує інтеграціями; логування (Serilog) з ротацією; аудит OrderHistory.  
- Стійкість інтеграцій: graceful fallback, ретраї, таймаути; idempotency для платежів.  
- Тести: XSS/CSRF/SQLi; рольові доступи; обмеження швидкості.  
- Критерій: проходить базові безпекові чек-лісти; стабільний під навантаженнями.  
## Етап 13: Локалізація та UX ✅
- UA/EN: i18n у фронтенді; переклад текстів/повідомлень/листів; форматування чисел/валют.  
- Адаптивність: mobile/desktop; доступність ARIA; спрощений checkout (≤5 кліків).  
- Критерій: повна локалізація і зручний UX.  
## Етап 14: CRUD-функції
- Стандартизувати REST CRUD для ключових сутностей; пагінація/сортування/фільтри; пошук; масові операції (bulk create/update/delete).  
- Оновлення: підтримати PUT/PATCH; ідемпотентність для повторних запитів; часткові оновлення без втрати даних.  
- Валідації DTO: унікальність, формати, референційна цілісність; повідомлення локалізовані UA/EN; єдині коди помилок.  
- RBAC: права на перегляд/створення/редагування/видалення за ролями; аудит createdBy/updatedBy/deletedBy; історія змін.  
- М’яке видалення: поля deletedAt/deletedBy; відновлення; заборона видалення сутностей, що використовуються у замовленнях/накладних.  
- Документація: описати всі CRUD у Swagger з прикладами; узгодити назви/статуси; консистентні відповіді.  
- Тести: юніт валідаторів і сервісів; інтеграційні CRUD для Products/Categories/Users/Companies/Promotions/Documents; конкурентні сценарії та блокування.  
- Критерій: всі CRUD працюють узгоджено; адміністратор і менеджер виконують операції самостійно.  
## Етап 15: Власний поштовий сервер
- Інфраструктура: власний SMTP-сервер (Postfix/Dovecot) у Docker; TLS (Let's Encrypt); налаштування SPF/DKIM/DMARC; PTR запис; окремий домен/піддомен.  
- Доставність: rate limiting, черга з ретраями; обробка bounce/complaints; захист від спаму (greylisting/blacklist).  
- Інтеграція: змінні оточення SMTP_*; підключення до модуля Нотифікацій з Етапу 7; фолбек на зовнішній провайдер; idempotency відправок.  
- Моніторинг: дашборд статусів пошти; логи; метрики доставності; алерти на збої/відмови.  
- Безпека: аутентифікація, обов’язковий TLS, обмеження доступу; секрети лише в env; захист від брутфорсу (fail2ban).  
- Оперування: управління аліасами/скриньками; обмеження розміру вкладень; архівування; політика зберігання.  
- Тести: перевірка SPF/DKIM/DMARC; відправлення на поштові сервіси (Gmail/Outlook/Yahoo) у sandbox; негативні кейси; ретраї/фолбеки.  
- Критерій: листи доставляються у inbox; стабільна робота під навантаженням; інтеграція з шаблонами працює.  
## Етап 16: Фінальний Лонч Чекліст
- Інтеграції активні з зеленими індикаторами; webhook Stripe працює; Nova Poshta ключ валідний; University API перевірено.  
- Документація доступна: Swagger; сторінки Політик/Повернень; контакти.  
- Е2Е сценарії: гості/користувачі/менеджери/адміністратори/суперадміни; негативні кейси оплати/доставки.  
- Стейджинг пройшов UAT; прод готовий; фічефлаги для поступового вмикання.  
- Пострелізні процедури: моніторинг, канали підтримки, швидке виправлення інцидентів.  
## Етап 17: DevOps і Реліз
- Docker Compose для локалки; Render деплой: API + фронт; налаштування ENV: STRIPE_ , NOVA_POSHTA_API_KEY, UNIVERSITY_API_KEY, SMTP_ .  
- Міграції БД; seed; щоденні бекапи; перевірка відновлення.  
- Моніторинг: лог-дашборд, алерти на критичні помилки/інтеграції.  
- CI/CD: lint/test/build; автодеплой staging → ручний gate → production.  
- Критерій: стабільний релізний цикл; швидке відновлення після збоїв.  
## Критичні залежності
- Stripe/Nova/University доступність: кешування/ретраї/ручний ввід; фічефлаги для вимкнення проблемної інтеграції.  
- Узгодженість Stock: транзакції + OutgoingDocuments як джерело істини; інвентаризаційні операції reason=INVENTORY.  
- Консистентність знижок: зберігати appliedPromotionId/discountAmount/finalPrice в OrderItems і OutgoingDocuments; звіти базуються на цих полях.  
## Тестове Покриття
- Юніт: Services (Order/Payment/Promotion/Shipping/Warehouse/User), валідатори, RBAC.  
- Інтеграційні: Stripe sandbox (checkout/session/webhook/refund), Nova Poshta (mock), University API (stub).  
- E2E: кошик → checkout → оплата/доставка; адмін: статуси, скасування, накладні, акції, компанії.  
- Навантаження: ≥500 активних користувачів; ≥50 оформлень/хв; кеш аналітики.  
- Безпека: XSS/CSRF/SQLi; рольові доступи; CSRF у формах; коректні CORS.



